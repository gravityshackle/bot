# --- data sources ---
databento>=0.34            # historical OHLCV (GLBX.MDP3)
ib_async>=1.0.3            # IBKR live/paper; maintained fork of ib_insync

# --- core ---
pandas>=2.2
numpy>=1.26
pyarrow>=15.0              # parquet cache
PyYAML>=6.0
python-dotenv>=1.0
pytz>=2024.1

# --- plotting (Phase 1/2 visual validation) ---
matplotlib>=3.8

# --- tests ---
pytest>=8.0
